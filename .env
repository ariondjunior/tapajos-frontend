# Use polling for file watching to avoid ENOSPC (inotify) errors on some Linux systems.
# This increases CPU usage but avoids the 'ENOSPC: System limit for number of file watchers reached' error.
CHOKIDAR_USEPOLLING=true

# Optional: reduce the debounce delay (milliseconds) to adjust responsiveness
CHOKIDAR_INTERVAL=100
